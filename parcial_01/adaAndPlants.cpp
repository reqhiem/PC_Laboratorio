#include <iostream>
using namespace std;

const int ms = 2e05 + 10;

int c,p,T,N,K, arr[ms];

int main(int argc, char const *argv[])
{
    cin >> T;
    while(c++ < T){
        cin >> N >> K;
        while(p < N) cin >> arr[p++];
    }
    return 0;
}
