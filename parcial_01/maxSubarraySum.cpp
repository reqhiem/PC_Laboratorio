#include <iostream>
#include <vector>

using namespace std;

const int ms = 2e05 + 10;

int arr[ms], N, a, b, i=0;

int main(int argc, char const *argv[])
{
    cin >> N >> a >> b;
    while(i < N) cin >> arr[i++];
    return 0;
}
